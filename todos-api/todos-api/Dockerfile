#выбираем начальный образ для сборки
FROM node:8-alpine

#выбираем рабочую папку внутри контейнера
WORKDIR /usr/src/app


#копируем список зависимостей
COPY package.json ./

#устанавливаем зависимости
RUN npm install

#копируем файлы с проектом в контейнер
COPY . .

#открываем порт
EXPOSE 8082

#запускаем сервер на ноде
CMD ["sh", "-c", "npm start" ]
